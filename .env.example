# ── Supabase ─────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key>
# Faqat serverda (Vercel env / Supabase secrets). Frontendga HECH QACHON chiqmaydi.
SUPABASE_SERVICE_ROLE_KEY=<service role key>

# ── Sayt ─────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://replio.uz
NEXT_PUBLIC_SUPPORT_TELEGRAM=replio_support

# ── checkout.uz (faqat serverda) ─────────────────────────
CHECKOUT_API_KEY=<checkout.uz API kalit>
# Ixtiyoriy: lokal test uchun soxta server (npm run e2e:mock-checkout)
# CHECKOUT_API_URL=http://127.0.0.1:4010/api/v1

# ── Telegram bot tokenlarini shifrlash kaliti (32 bayt, hex) ──
# openssl rand -hex 32
BOT_TOKEN_KEY=<64 ta hex belgi>
